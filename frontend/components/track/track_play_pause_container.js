import { connect } from 'react-redux';
import TrackPlayPause from './track_play_pause.jsx';

const mSTP = state => ({});

const mDTP = dispatch => ({});

export default connect(mSTP, mDTP)(TrackPlayPause);